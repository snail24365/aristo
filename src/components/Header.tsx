import { css } from '@emotion/react';

export default function Header() {
  return (
    <div
      css={{
        color: 'darkorchid',
        backgroundColor: 'lightgray',
      }}>
      This is darkorchid.
    </div>
  );
}
